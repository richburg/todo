Description:
-------------
Easily manage your TODOs directly from the CLI

Installation:
-------------
1. Clone the repository
2. Navigate to the project directory
3. Run `pipx install .` to build and add it to your PATH

Usage:
-------------
You can use `--help` to get a list of commands
Example:
    $ todo --help
    > [list of commands]
